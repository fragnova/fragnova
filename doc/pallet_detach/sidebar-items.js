window.SIDEBAR_ITEMS = {"constant":[["KEY_TYPE","Defines application identifier for crypto keys of this module."]],"enum":[["DetachCollection","Enum representing a collection of “detachable things” that the User wants to detach from the Fragnova Blockchain"],["DetachCollectionType","Enum representing the different Collection Types"],["DetachHash","Enum representing a “detachable thing” (i.e a Proto-Fragment or a Fragment Instance) that the User wants to detach from the Fragnova Blockchain"],["SupportedChains","Possible Blockchains into which a Proto-Fragment can be detached"]],"mod":[["crypto","Based on the above `KeyTypeId` we need to generate a pallet-specific crypto type wrappers. We can use from supported crypto kinds (`sr25519`, `ed25519` and `ecdsa`) and augment the types with this pallet-specific identifier."],["pallet","The module that hosts all the FRAME types needed to add this pallet to a runtime."]],"struct":[["DetachInternalData","Payload represents information about a collection of “detachable thing“s (see enum `DetachHash` to see what type of collections can be detached) that will be detached"],["DetachRequest","Struct that represents a request to detach a collection of “detachable thing“s (see enum `DetachHashes` to see what type of collections can be detached) from the Fragnova Blockchain"],["ExportData","Struct that contains information about a “detached thing” (e.g a detached Proto-Fragment or a detached Fragment Instance) that was detached from the Fragnova Blockchain"]],"trait":[["WeightInfo","Weight functions needed for pallet_detach."]]};