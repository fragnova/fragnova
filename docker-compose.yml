version: "3.2"

services:
  dev:
    container_name: node-entities
    image: paritytech/ci-linux:974ba3ac-20201006
    working_dir: /var/www/node-entities
    ports:
      - "9944:9944"
    environment:
      - CARGO_HOME=/var/www/node-entities/.cargo
    volumes:
      - .:/var/www/node-entities
      - type: bind
        source: ./.local
        target: /root/.local
    command: bash -c "cargo build --release && ./target/release/node-entities --dev --ws-external"
